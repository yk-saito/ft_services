server  {
    listen 80 default_server;
    listen [::]:80  default_server;

    root /var/www/html;

    location  / {
        
        # return 404;
    }

    location = /404.html {
        internal;
    }
}